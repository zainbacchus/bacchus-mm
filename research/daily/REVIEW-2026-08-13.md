# Daily review: 2026-08-13

Data source: `research/daily/REVIEW-DATA-2026-08-13.md` (26h unsegmented pull,
generated 2026-08-13T12:11:37Z). Compared against yesterday's
`REVIEW-2026-08-12.md`, `ROADMAP.md` STATUS 2026-08-12, and GitHub issues
#9, #12, #13, #14 (all PULSE ALARM).

Environment note: same one-time fix as prior days (`cryptography` installed
via the system package manager was missing `_cffi_backend`). Fixed with
`pip install --user --force-reinstall --no-deps cryptography` plus
`pip install --user cryptography certifi cffi`. No ad hoc API calls; every
number below comes from `research/daily_review.py`.

Config-change boundary: none inside this 26h window. The last commit
touching `config.yaml` (`f9a0a3e`, the M7 side-tripwire params) landed
2026-08-12T04:13:49Z, before this window opened (~2026-08-12T10:11Z), so the
full-26h cut and a since-change cut would be identical; not re-run
separately.

## The overnight event (read before the numbers below)

**This pull very likely measures a bot that halted partway through the
window, for the third night running.** Two new PULSE ALARM issues fired
since yesterday's review, both still open:

- **#13** (02:18Z): equity down $24.49 vs yesterday's $496.33 baseline
  (threshold $15), bot still filling normally at that point.
- **#14** (08:21Z): **no fills in 4h** + equity down $34.21 vs baseline,
  "close to the $30 cumulative kill-switch trip." The pulse routine's own
  interpretation: "reads most consistent with the kill switch having
  tripped and halted the bot... an outage alone wouldn't explain the
  drawdown."

This review's own pull independently confirms both halves: last account
fill **543 minutes (~9.1h) before this pull** (well past the routine's
2h staleness alarm), and settled PnL of **-$34.21**, matching issue #14's
figure almost exactly. I cannot see `data/HALTED` or fly logs from here
(REST-only, per the routine's hard rules), so I cannot confirm kill-switch
vs. crash vs. deliberate stop, but the size and shape of the drawdown
immediately preceding the stale-fill window makes a kill-switch trip the
most likely of the three. Issues **#9** and **#12** from the two prior
nights (2026-08-12, 2026-08-11) also remain open and unacknowledged, so
there are now **four consecutive unresolved PULSE ALARM issues**.

Separately: `LEDGER.md` shows 2026-08-12 finalized at **-$32.24 net**, far
worse than yesterday's own partial estimate of -$15.22 (a **-$17.02**
further downward revision, the largest yet recorded, and evidence the bot
did resume trading after the M7 PR merged at 2026-08-12T14:11:59Z and then
lost significantly more before stopping again). This is consistent with a
second stop-and-restart-and-stop-again cycle inside the same 24h period the
prior review already flagged as "restart gated on M7."

## Headline

TOTAL REALIZED vs SETTLEMENT (full 26h): **-$34.21, -3.42c/contract, ~se
1.58c** on 1001.3 settled contracts, **about 2.16 standard errors from
zero**. This is the first headline this routine has recorded that clears
the ~2se bar in either direction (prior worst was -1.52c/ct at ~1.15se on
2026-08-12). No since-change cut applies (see above).

## Equity check vs yesterday's baseline

Yesterday's review recorded total account equity as $494.87 balance + $1.46
in positions = **$496.33**. Today: $460.67 balance + $1.44 in positions =
**$462.11**, a change of **-$34.22**. Settled PnL over the same ~26h is
-$34.21 (gap -$0.01), the tightest match on record, consistent with the
machine sitting stopped for a large part of the back half of the window
with no resting exposure drifting between reviews (same shape as the
2026-08-12 review's "tightest match" note, now tighter still).

## Ledger glance (trailing week)

08-12 finalized at **-$32.24 net** (2249 more fills settled since
yesterday's partial read), the **largest downward revision this ledger has
recorded** (-$17.02 vs. yesterday's -$15.22 partial estimate; the prior
largest revision was -$5.16 on 08-10). It is now the single worst day in
the ledger, ahead of 08-11's -$21.52. 08-13 (today, partial, machine
apparently stopped since ~03:09Z): **-$17.19 net** on 177 fills / 162.9
contracts / $98.53 volume, already negative despite covering under 3 hours
of live trading. Cumulative net PnL: +$17.59 (08-09 peak, revised) ->
-$12.81 (08-11) -> **-$45.05** (08-12) -> **-$62.24** (08-13, partial) -
an ~$79.83 round trip from peak in four days, three of those days now
"hostile nights." Volume on 08-12 ($726.28) was under half of 08-11's
($1,588.66); fees stayed trivial both days ($0.0003, $0.0000).

## Per-series verdicts vs current config (`fifteen.series`: BTC, ETH, GOLD, SILVER, WTI, XRP)

Full 26h, ~se = 50/sqrt(contracts):

| series | contracts | pnl $ | c/ct | ~se | verdict |
|---|---|---|---|---|---|
| KXETH15M | 200.2 | -14.84 | -7.41 | 3.53c | **negative, clears ~2.10se** |
| KXWTI15M | 137.4 | -9.83 | -7.16 | 4.27c | inconclusive (~1.68se) |
| KXSILVER15M | 150.9 | -9.78 | -6.48 | 4.07c | inconclusive (~1.59se) |
| KXGOLD15M | 146.9 | -4.79 | -3.26 | 4.13c | inconclusive (~0.79se) |
| KXXRP15M | 148.9 | +0.67 | +0.45 | 4.10c | flat (~0.11se) |
| KXBTC15M | 217.0 | +4.36 | +2.01 | 3.39c | inconclusive (~0.59se) |

**KXETH15M is the first series this routine has seen individually clear
~2se** (prior high-water mark was KXGOLD15M at ~0.85se on 08-12). One day is
not two; per the routine's own standing caution against single-day series
calls, this is a flag to watch tomorrow, not a drop proposal. WTI and
SILVER are both meaningfully negative (~1.6se) without clearing, and unlike
the last two reviews the sign pattern is now mixed (4 of 6 negative, BTC
and XRP positive) rather than uniform, which argues against a single
account-wide macro lean explaining tonight's series-level picture as
cleanly as it did the last two nights.

**KXXRP15M** has now completed its 4th day of real volume under the
ROADMAP item 6 re-entry probe (+0.03, +0.58, -0.87, +0.45 c/ct across the
four days) - it has reached the "3-4 settled days" decision gate, but every
individual day and the rough average (~+0.05c/ct) stay inconclusive/flat.
No keep/drop verdict follows; flagging that the gate has been reached in
case the next interactive session wants to pool the four days formally.

## Tilt evidence (M1, `fifteen.tilt_tail_threshold: 0.65`)

By price band of the side taken, full 26h:

| band | contracts | c/ct | ~se | verdict |
|---|---|---|---|---|
| 0.35-0.65 | 729.3 | -2.37 | 1.85c | inconclusive (~1.28se) |
| 0.65-0.90 | 167.0 | -9.81 | 3.87c | **negative, clears ~2.54se** |
| 0.90-1.00 | 105.0 | -0.51 | 4.88c | flat (~0.10se) |

**This is the first band-level read to clear significance since day 1 of
the walk-down (0.65-0.90 at +15.08c/ct, ~6.0se) - and it is now in the
opposite direction.** The 0.65-0.90 band's day-by-day trajectory across the
five days measured: +15.08 -> +0.18 -> -0.96 -> -0.02 -> **-9.81** c/ct.
What looked like decaying noise around zero for three days has now produced
a second significant read, on the loss side. I cannot tell from settlement
data alone whether these are trades the tilt lever actively shaped (would
have sold the favorite / bought the longshot absent M1) or ordinary
mid-band joins that happen to sit in this price range; `join_touch.py`
telemetry (which this routine cannot see) would be needed to attribute this
to the lever itself rather than to the same broad drawdown hitting every
cut of the data tonight. Flagging for the next interactive session rather
than proposing a threshold move on this alone.

## Open-delay invariants (`min_seconds_after_open: 90`)

By-minutes table has **no minute-14 bucket** - invariant holds.

**Minute-13 is clearly negative: -5.40c/ct on 391.0 contracts, ~2.14se**,
also the highest-volume bucket. Per the standing rule (research/ATTRIBUTION
-WINDOW-OPEN-2026-08-11.md and the last three reviews' rule 7b): later
minutes are mixed, not clearly negative (minute-12 -2.88c/ct at ~0.86se,
minute-9 -12.23c/ct at ~1.53se on only 39 ct, minute-7 actually **positive**
at +11.98c/ct, ~1.94se) - closer to "clearly negative at 13, not clearly
negative afterward" than to a spreading toxic regime. This matches the
migration case the standing rule calls out: propose the **join_depth
posting filter** as the next single variable for an interactive session to
design and build (this requires a code change in `strategy/join_touch.py`
or `fifteen.py`, outside this routine's config-only scope, so no config
diff follows - same treatment as the 08-11 review's equivalent flag).

## By-side divergence (drift-selection signature)

| side | contracts | pnl $ | c/ct | ~se |
|---|---|---|---|---|
| buys | 448.7 | -42.15 | -9.39 | 2.36c (~3.98se) |
| sells | 552.5 | +7.93 | +1.44 | 2.13c (~0.68se) |

**This is not the same shape as the last two divergence events.** On
08-11 and 08-12 both sides individually cleared zero in opposite
directions (symmetric two-sided farming). Tonight only **buys** clear
zero, at ~4.0se, while sells sit near flat (~0.68se) - an asymmetric,
one-sided bleed. This is, as far as this routine can tell from REST data,
**exactly the pattern M7 (the per-side rolling-expectancy tripwire) was
built to catch**: one side bleeding at scale (-9.39c/ct on 448.7 contracts,
well past the configured -8c/ct on >=100ct trigger) while the other side
stays healthy. Whether M7 actually fired tonight is a question this
routine cannot answer: it has no visibility into `side_tripwire_trip` /
`side_tripwire_resume` telemetry, and more basically it is not certain the
fly deployment was ever redeployed with the M7 code after PR #11 merged
(2026-08-12T14:11:59Z) - `CLAUDE.md` is explicit that "merging does not
deploy." Recommend the next interactive session with fly-DB access check,
in order: (1) whether the running binary includes M7 at all, (2) if so,
whether `side_tripwire_trip` fired on the buy side tonight and if not why
the rolling 2h window never crossed the trigger despite the full-26h
aggregate clearing it by a wide margin.

## 15M series discovery

No new 15M series discovered (19 known, `known_15m_series.json` unchanged).

## Release calendar maintenance

CPI (2026-08-12, already passed) and the next CPI (2026-09-11) and FOMC
(2026-09-16) dates remain populated and current in `config.yaml
fifteen.release_calendar`. Nothing stale to flag.

## Anomaly checks

- Taker fills: **0**. Clean (post-only invariant holding).
- Total fees: **$0.0002** on 1001.3 contracts. Clean.
- Windows with |c/ct| > 20 on more than 10 contracts: **none**. Largest
  magnitudes (KXSILVER15M-26AUG122145-45 at -86.33c/ct, KXETH15M
  -26AUG122300-00 at -60.75c/ct) are on 3-4 contracts, under the
  10-contract floor.
- Minute-14 bucket: **absent**, invariant holds (see above).
- **Stale last-account fill: 543 min (~9.1h) before this pull.** Past the
  routine's 2h alarm threshold. Fully covered above (probable kill-switch
  halt, third consecutive night); not a new/separate finding from the
  overnight-event section, reporting per house rule.
- Equity vs settled PnL: covered above, tightest gap on record (-$0.01).
  No unexplained drift beyond the settled-PnL-driven drop itself.
- **Housekeeping**: GitHub issues #9 (08-12, 02:19Z), #12 (08-12, 08:21Z),
  #13 (08-13, 02:18Z), and #14 (08-13, 08:21Z) - all PULSE ALARM - remain
  **open**. That is now four consecutive unresolved alarms spanning three
  nights. Not a routine action (this routine does not triage issues), but
  flagging loudly since the count is climbing, not clearing.

## Proposals

**Proposal 1 (hold): keep `fifteen.series` at `[KXBTC15M, KXETH15M,
KXGOLD15M, KXSILVER15M, KXWTI15M, KXXRP15M]`, no change.**

Evidence: KXETH15M is the first series to individually clear ~2se negative
(-7.41c/ct on 200.2 ct, ~2.10se) in this routine's history, but one day is
not the two-day pattern the routine requires before a drop call, and
tonight's per-series signs are mixed (4 of 6 negative) rather than uniform,
unlike the last two nights. KXWTI15M and KXSILVER15M are both notably
negative (~1.6-1.7se) without clearing. KXXRP15M has reached ROADMAP item
6's 3-4 day re-entry gate with all four daily reads inconclusive/mixed sign
(+0.03/+0.58/-0.87/+0.45 c/ct); no keep/drop verdict follows.

```diff
--- config.yaml
+++ config.yaml
@@ fifteen.series
   series:
     - KXBTC15M
     - KXETH15M
     - KXGOLD15M
     - KXSILVER15M
     - KXWTI15M
     - KXXRP15M
+  # 2026-08-13 daily review: held. KXETH15M cleared ~2.10se negative for
+  # the first time in this routine's history (-7.41c/ct on 200.2 ct) - a
+  # trend to watch tomorrow, not a one-day drop call. WTI/SILVER both
+  # ~1.6-1.7se negative without clearing. XRP reached the 3-4 day re-entry
+  # gate (+0.03/+0.58/-0.87/+0.45 c/ct across four days) still
+  # inconclusive; no verdict.
```

**Proposal 2 (hold, flagged): keep `fifteen.tilt_tail_threshold` at
`0.65`, no change.**

Evidence: the 0.65-0.90 band produced its first significant read since
day 1 of the walk-down, and it is now negative (-9.81c/ct, ~2.54se),
completing a five-day trajectory of +15.08 / +0.18 / -0.96 / -0.02 /
-9.81 c/ct. This is a notable reversal worth flagging loudly, but this
routine cannot see `join_touch.py` telemetry and so cannot tell whether
these are tilt-shaped trades or ordinary joins caught in the same broad
drawdown as every other cut tonight. No threshold move proposed without
that attribution; recommend the next interactive session pull M1 telemetry
specifically for this band before any lever change.

```diff
--- config.yaml
+++ config.yaml
@@ fifteen.tilt_tail_threshold
   tilt_tail_threshold: 0.65
+  # 2026-08-13 daily review: held. 0.65-0.90 band read -9.81c/ct (~2.54se),
+  # its first significant read since day 1 of the walk-down (+15.08c/ct)
+  # and now in the opposite direction. Cannot attribute to the tilt lever
+  # itself without join_touch.py telemetry (out of this routine's REST-only
+  # visibility); flagged for interactive attribution before any lever
+  # change.
```

**Proposal 3 (hold, informational, no config diff): third consecutive
hostile night, probable kill-switch halt, and an unresolved question about
whether M7 was even live.**

Evidence: two new PULSE ALARM issues (#13, #14) plus this routine's own
pull (last fill 543 min ago, settled PnL -$34.21 matching issue #14's
figure almost exactly) point to a kill-switch halt, the third such event in
three nights, with four PULSE issues (#9, #12, #13, #14) now open and
unacknowledged. `LEDGER.md` shows 08-12 revised to -$32.24 (its largest
downward revision on record), consistent with the bot resuming after the
M7 PR merged (2026-08-12T14:11:59Z) and then losing significantly more
before stopping again. Tonight's by-side divergence (buys -9.39c/ct on
448.7 ct at ~3.98se, sells flat) is, on REST data alone, exactly the shape
M7 was built to catch - but this routine cannot confirm the fly deployment
was ever redeployed with M7's code, nor see `side_tripwire_trip` telemetry
to know whether it fired. Separately, minute-13 turned clearly negative
(-5.40c/ct, ~2.14se) while later minutes did not, the migration case the
standing rule (7b) flags for a join_depth posting filter - a code change,
so no config diff follows here either.

No config diff (informational only). Recommends an interactive session
before any restart: confirm halt cause (kill switch vs. crash), confirm
whether M7 was deployed and whether it fired, and triage the four open
PULSE issues.

---

Merging does not deploy. The owner must redeploy in the fly.io UI to apply
config changes.
